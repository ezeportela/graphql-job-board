type Query {
  jobs: [Job]
  job(id: ID!): Job

  companies: [Company]
  company(id: ID!): Company
}

type Company {
  id: ID!
  name: String
  description: String
}

type Job {
  id: ID!
  title: String
  company: Company
  description: String
}
